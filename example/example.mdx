# Example document

Local MDX viewer smoke test.

<Callout
  tone="decision"
  body="Write MDX anywhere in your repo (`docs/`, `plans/`, etc.). The viewer stays global; open with `mdxv <path>`."
/>

<FileTree
  title="Scope"
  entries={[
    { path: "server/routes/tasks.ts", change: "modified" },
    { path: "server/routes/tasks.test.ts", change: "added", note: "Covers create path" },
  ]}
/>

<Tabs>
  <Tab id="handler" label="tasks.ts">
    <Diff
      id="diff-handler-unified"
      summary="Unified view (default)"
      filename="server/routes/tasks.ts"
      language="ts"
      before={`import { Router } from "express";\nimport { list } from "./handlers";\n\nconst router = Router();\n\nrouter.get("/api/tasks", list);\nrouter.delete("/api/tasks/:id", remove);\n\nexport { router };`}
      after={`import { Router } from "express";\nimport { create, list } from "./handlers";\n\nconst router = Router();\n\nrouter.get("/api/tasks", list);\nrouter.post("/api/tasks", create); // creates a task\n\nexport { router };`}
    />
    <Diff
      id="diff-handler-split"
      mode="split"
      summary="Split view"
      filename="server/routes/tasks.ts"
      language="ts"
      before={`import { Router } from "express";\nimport { list } from "./handlers";\n\nconst router = Router();\n\nrouter.get("/api/tasks", list);\nrouter.delete("/api/tasks/:id", remove);\n\nexport { router };`}
      after={`import { Router } from "express";\nimport { create, list } from "./handlers";\n\nconst router = Router();\n\nrouter.get("/api/tasks", list);\nrouter.post("/api/tasks", create); // creates a task\n\nexport { router };`}
    />
  </Tab>
  <Tab id="test" label="tasks.test.ts">
    <Code
      filename="server/routes/tasks.test.ts"
      language="ts"
      code={`import { createTask } from "./tasks";\n\ntest("creates a task", async () => {\n  const result = await createTask({ title: "Ship" });\n  expect(result.id).toBeDefined(); // happy path: id returned\n});`}
    />
  </Tab>
</Tabs>

<Code
  language="bash"
  code={`mdxv docs/guide.mdx\nmdxv plans/example/plan.mdx`}
  caption="Open any MDX file from the project root"
/>

<Mermaid
  source={`sequenceDiagram\n  participant Client\n  participant API\n  Client->>API: POST /api/tasks\n  API-->>Client: 201 Created`}
  caption="Create task flow"
/>

## Implementation

- [ ] Add POST route
- [ ] Add handler test (use existing test helpers)
- [ ] Update API notes
